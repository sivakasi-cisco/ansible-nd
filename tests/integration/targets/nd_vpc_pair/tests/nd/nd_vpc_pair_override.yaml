- name: ND vPC pair override tests
  hosts: nd
  gather_facts: false
  tasks:
    ##############################################
    ##                 SETUP                    ##
    ##############################################

    - name: OVERRIDE - Test Entry Point - [nd_vpc_pair]
      ansible.builtin.debug:
        msg:
          - "----------------------------------------------------------------"
          - "+        Executing Override Tests - [nd_vpc_pair]            +"
          - "----------------------------------------------------------------"
      tags: override

    ##############################################
    ##      Setup Internal TestCase Variables  ##
    ##############################################

    - name: OVERRIDE - Setup Internal TestCase Variables
      ansible.builtin.set_fact:
        deploy_local: true
        test_fabric: "{{ fabric_name }}"
        test_switch1: "{{ switch1_serial }}"
        test_switch2: "{{ switch2_serial }}"
        test_fabric_type: "{{ fabric_type | default('LANClassic') }}"
        test_data_overridden:
          vpc_pair_initial_conf:
            - peer1_switch_id: "{{ switch1_serial }}"
              peer2_switch_id: "{{ switch2_serial }}"
              use_virtual_peer_link: true
          vpc_pair_overridden_conf:
            - peer1_switch_id: "{{ switch1_serial }}"
              peer2_switch_id: "{{ switch2_serial }}"
              use_virtual_peer_link: false
      tags: override

    ##############################################
    ##               OVERRIDE                 ##
    ##############################################

    # TC1 - Override with a new vPC switch pair
    - name: OVERRIDE - TC1 - DELETE - Clean up any existing vPC pairs
      cisco.nd.nd_vpc_pair:
        fabric_name: "{{ test_fabric }}"
        state: deleted
      tags: override

    - name: OVERRIDE - TC1 - OVERRIDE - Create vPC pair using override state
      cisco.nd.nd_vpc_pair:
        fabric_name: "{{ test_fabric }}"
        state: overridden
        config: "{{ test_data_overridden.vpc_pair_initial_conf }}"
      register: result
      tags: override

    - name: OVERRIDE - TC1 - ASSERT - Check if changed flag is true
      ansible.builtin.assert:
        that:
          - result.failed == false
      tags: override

    - name: OVERRIDE - TC1 - GATHER - Get vPC pair state in ND
      cisco.nd.nd_vpc_pair:
        state: gathered
        fabric_name: "{{ test_fabric }}"
        config:
          - peer1_switch_id: "{{ test_switch1 }}"
            peer2_switch_id: "{{ test_switch2 }}"
      register: verify_result
      tags: override

    - name: OVERRIDE - TC1 - ASSERT - Verify vPC pair state in ND
      ansible.builtin.assert:
        that:
          - verify_result.failed == false
          - '(verify_result.gathered.vpc_pairs | length) == 1'
      tags: override

    # TC2 - Override with same vPC switch pair with changes
    - name: OVERRIDE - TC2 - OVERRIDE - Override vPC pair with changes
      cisco.nd.nd_vpc_pair: &conf_overridden
        fabric_name: "{{ test_fabric }}"
        state: overridden
        config: "{{ test_data_overridden.vpc_pair_overridden_conf }}"
      register: result
      tags: override

    - name: OVERRIDE - TC2 - ASSERT - Check if changed flag is true for LANClassic
      ansible.builtin.assert:
        that:
          - result.failed == false
      when: test_fabric_type == "LANClassic"
      tags: override

    - name: OVERRIDE - TC2 - ASSERT - Check if changed flag is false for VXLANFabric
      ansible.builtin.assert:
        that:
          - result.failed == false
      when: test_fabric_type == "VXLANFabric"
      tags: override

    - name: OVERRIDE - TC2 - GATHER - Get vPC pair state in ND
      cisco.nd.nd_vpc_pair:
        state: gathered
        fabric_name: "{{ test_fabric }}"
        config:
          - peer1_switch_id: "{{ test_switch1 }}"
            peer2_switch_id: "{{ test_switch2 }}"
      register: verify_result
      tags: override

    - name: OVERRIDE - TC2 - ASSERT - Verify vPC pair state in ND
      ansible.builtin.assert:
        that:
          - verify_result.failed == false
          - '(verify_result.gathered.vpc_pairs | length) == 1'
          - verify_result.gathered.vpc_pairs[0].useVirtualPeerLink == false
      when: test_fabric_type == "LANClassic"
      tags: override

    # TC3 - Idempotence test
    - name: OVERRIDE - TC3 - conf - Idempotence
      cisco.nd.nd_vpc_pair: *conf_overridden
      register: result
      tags: override

    - name: OVERRIDE - TC3 - ASSERT - Check if changed flag is false
      ansible.builtin.assert:
        that:
          - result.changed == false
          - result.failed == false
      tags: override

    # TC4 - Override existing vPC pair with no config (delete all)
    - name: OVERRIDE - TC4 - OVERRIDE - Delete all vPC pairs via override with no config
      cisco.nd.nd_vpc_pair:
        fabric_name: "{{ test_fabric }}"
        state: overridden
        config: []
      register: result
      tags: override

    - name: OVERRIDE - TC4 - ASSERT - Check if deletion successful
      ansible.builtin.assert:
        that:
          - result.failed == false
      tags: override

    - name: OVERRIDE - TC4 - GATHER - Get vPC pair state in ND
      cisco.nd.nd_vpc_pair:
        state: gathered
        fabric_name: "{{ test_fabric }}"
      register: verify_result
      tags: override

    - name: OVERRIDE - TC4 - ASSERT - Verify vPC pair deletion via override
      ansible.builtin.assert:
        that:
          - verify_result.failed == false
          - '(verify_result.gathered.vpc_pairs | length) == 0'
      tags: override

    # TC5 - Gather to verify deletion
    - name: OVERRIDE - TC5 - GATHER - Verify vPC pair deletion
      cisco.nd.nd_vpc_pair:
        fabric_name: "{{ test_fabric }}"
        state: gathered
        config:
          - peer1_switch_id: "{{ test_switch1 }}"
            peer2_switch_id: "{{ test_switch2 }}"
      register: result
      until:
        - '(result.gathered.vpc_pairs | length) == 0'
      retries: 30
      delay: 5
      tags: override

    # TC6 - Override with no vPC pair and no config (should be no-op)
    - name: OVERRIDE - TC6 - OVERRIDE - Override with no vPC pairs (no-op)
      cisco.nd.nd_vpc_pair:
        fabric_name: "{{ test_fabric }}"
        state: overridden
        config: []
      register: result
      tags: override

    - name: OVERRIDE - TC6 - ASSERT - Check if no change occurred
      ansible.builtin.assert:
        that:
          - result.failed == false
      tags: override

    - name: OVERRIDE - TC6 - GATHER - Get vPC pair state in ND
      cisco.nd.nd_vpc_pair:
        state: gathered
        fabric_name: "{{ test_fabric }}"
      register: verify_result
      tags: override

    - name: OVERRIDE - TC6 - ASSERT - Verify no-op override
      ansible.builtin.assert:
        that:
          - verify_result.failed == false
          - '(verify_result.gathered.vpc_pairs | length) == 0'
      tags: override

    ##############################################
    ##                 CLEAN-UP                 ##
    ##############################################

    - name: OVERRIDE - END - remove vPC pairs
      cisco.nd.nd_vpc_pair:
        fabric_name: "{{ test_fabric }}"
        state: deleted
      when: cleanup_at_end | default(true)
      tags: override
