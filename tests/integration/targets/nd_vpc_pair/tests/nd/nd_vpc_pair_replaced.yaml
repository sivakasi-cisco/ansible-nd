- name: ND vPC pair replaced tests
  hosts: nd
  gather_facts: false
  tasks:
    ##############################################
    ##                 SETUP                    ##
    ##############################################

    - name: REPLACED - Test Entry Point - [nd_vpc_pair]
      ansible.builtin.debug:
        msg:
          - "----------------------------------------------------------------"
          - "+         Executing Replaced Tests - [nd_vpc_pair]             +"
          - "----------------------------------------------------------------"
      tags: replaced

    ##############################################
    ##      Setup Internal TestCase Variables  ##
    ##############################################

    - name: REPLACED - Setup Internal TestCase Variables
      ansible.builtin.set_fact:
        deploy_local: true
        test_fabric: "{{ fabric_name }}"
        test_switch1: "{{ switch1_serial }}"
        test_switch2: "{{ switch2_serial }}"
        test_fabric_type: "{{ fabric_type | default('LANClassic') }}"
        test_data_replaced:
          vpc_pair_initial_conf:
            - peer1_switch_id: "{{ switch1_serial }}"
              peer2_switch_id: "{{ switch2_serial }}"
              use_virtual_peer_link: true
          vpc_pair_replaced_conf:
            - peer1_switch_id: "{{ switch1_serial }}"
              peer2_switch_id: "{{ switch2_serial }}"
              use_virtual_peer_link: false
      tags: replaced

    ##############################################
    ##                REPLACED                  ##
    ##############################################

    # TC1 - Create initial vPC pair using replaced state
    - name: REPLACED - TC1 - DELETED - Clean up any existing vPC pairs
      cisco.nd.nd_vpc_pair:
        fabric_name: "{{ test_fabric }}"
        state: deleted
      tags: replaced

    - name: REPLACED - TC1 - REPLACED - Create vPC pair using replaced state
      cisco.nd.nd_vpc_pair:
        fabric_name: "{{ test_fabric }}"
        state: replaced
        config: "{{ test_data_replaced.vpc_pair_initial_conf }}"
      register: result
      tags: replaced

    - name: REPLACED - TC1 - ASSERT - Check if changed flag is true
      ansible.builtin.assert:
        that:
          - result.failed == false
      tags: replaced

    - name: REPLACED - TC1 - QUERY - Get vPC pair state in ND
      cisco.nd.nd_vpc_pair:
        state: gathered
        fabric_name: "{{ test_fabric }}"
        config:
          - peer1_switch_id: "{{ test_switch1 }}"
            peer2_switch_id: "{{ test_switch2 }}"
      register: verify_result
      tags: replaced

    - name: REPLACED - TC1 - ASSERT - Verify vPC pair state in ND
      ansible.builtin.assert:
        that:
          - verify_result.failed == false
          - '(verify_result.gathered.vpc_pairs | length) == 1'
      tags: replaced

    # TC2 - Replace vPC pair configuration
    - name: REPLACED - TC2 - REPLACED - Replace vPC pair configuration
      cisco.nd.nd_vpc_pair: &conf_replaced
        fabric_name: "{{ test_fabric }}"
        state: replaced
        config: "{{ test_data_replaced.vpc_pair_replaced_conf }}"
      register: result
      tags: replaced

    - name: REPLACED - TC2 - ASSERT - Check if changed flag is true for LANClassic
      ansible.builtin.assert:
        that:
          - result.failed == false
      when: test_fabric_type == "LANClassic"
      tags: replaced

    - name: REPLACED - TC2 - ASSERT - Check if changed flag is false for VXLANFabric
      ansible.builtin.assert:
        that:
          - result.failed == false
      when: test_fabric_type == "VXLANFabric"
      tags: replaced

    - name: REPLACED - TC2 - QUERY - Get vPC pair state in ND
      cisco.nd.nd_vpc_pair:
        state: gathered
        fabric_name: "{{ test_fabric }}"
        config:
          - peer1_switch_id: "{{ test_switch1 }}"
            peer2_switch_id: "{{ test_switch2 }}"
      register: verify_result
      tags: replaced

    - name: REPLACED - TC2 - ASSERT - Verify vPC pair state in ND
      ansible.builtin.assert:
        that:
          - verify_result.failed == false
          - '(verify_result.gathered.vpc_pairs | length) == 1'
          - verify_result.gathered.vpc_pairs[0].useVirtualPeerLink == false
      when: test_fabric_type == "LANClassic"
      tags: replaced

    # TC3 - Idempotence test
    - name: REPLACED - TC3 - conf - Idempotence
      cisco.nd.nd_vpc_pair: *conf_replaced
      register: result
      tags: replaced

    - name: REPLACED - TC3 - ASSERT - Check if changed flag is false
      ansible.builtin.assert:
        that:
          - result.failed == false
      tags: replaced

    ##############################################
    ##                 CLEAN-UP                 ##
    ##############################################

    - name: REPLACED - END - remove vPC pairs
      cisco.nd.nd_vpc_pair:
        fabric_name: "{{ test_fabric }}"
        state: deleted
      when: cleanup_at_end | default(true)
      tags: replaced
